<Window x:Class="PrintPilotDesk.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PrintPilot 设置"
        Width="440" Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1C2026"
        Foreground="#ECF0F4"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display">
  <Window.Resources>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="#ECF0F4"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Background" Value="#252A32"/>
      <Setter Property="Foreground" Value="#ECF0F4"/>
      <Setter Property="CaretBrush" Value="#2DD4BF"/>
      <Setter Property="BorderBrush" Value="#3A414B"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="8,6"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6">
              <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="PasswordBox">
      <Setter Property="Background" Value="#252A32"/>
      <Setter Property="Foreground" Value="#ECF0F4"/>
      <Setter Property="CaretBrush" Value="#2DD4BF"/>
      <Setter Property="BorderBrush" Value="#3A414B"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="8,6"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="PasswordBox">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6">
              <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="#ECF0F4"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal">
              <Border x:Name="box" Width="18" Height="18" CornerRadius="4"
                      Background="#252A32" BorderBrush="#3A414B" BorderThickness="1"
                      VerticalAlignment="Center">
                <Path x:Name="mark" Data="M3,9 L7,13 L15,4" Stroke="#2DD4BF" StrokeThickness="2"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"
                      Width="16" Height="16" Stretch="None"/>
              </Border>
              <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"
                                TextBlock.Foreground="{TemplateBinding Foreground}"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="mark" Property="Visibility" Value="Visible"/>
                <Setter TargetName="box" Property="BorderBrush" Value="#2DD4BF"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="box" Property="BorderBrush" Value="#9AA3AD"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Border Background="#1C2026" Padding="20">
    <DockPanel>
      <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
        <Button Content="测试连接" Style="{StaticResource GhostBtn}" Click="OnTest" Margin="0,0,8,0"/>
        <Button Content="保存" Style="{StaticResource PrimaryBtn}" Click="OnSave" IsDefault="True"/>
      </StackPanel>
      <StackPanel>
        <TextBlock Text="任务栏托盘" FontSize="18" FontWeight="SemiBold" Foreground="#ECF0F4"/>
        <TextBlock Text="令牌用网站设置页里的 AI 令牌。保存后开始拉机台和空气。"
                   Foreground="#9AA3AD" TextWrapping="Wrap" Margin="0,4,0,14"/>

        <TextBlock Text="Hub 网址" FontSize="12" Foreground="#9AA3AD"/>
        <TextBox x:Name="UrlBox" Margin="0,4,0,12"/>

        <TextBlock Text="AI 令牌" FontSize="12" Foreground="#9AA3AD"/>
        <PasswordBox x:Name="TokenBox" Margin="0,4,0,12"/>

        <TextBlock Text="刷新间隔（秒）" FontSize="12" Foreground="#9AA3AD"/>
        <DockPanel Margin="0,4,0,12">
          <TextBlock x:Name="PollLabel" DockPanel.Dock="Right" Width="36" Text="30"
                     VerticalAlignment="Center" Foreground="#2DD4BF"/>
          <Slider x:Name="PollSlider" Minimum="5" Maximum="120" TickFrequency="5"
                  IsSnapToTickEnabled="True" Value="30" ValueChanged="OnPollChanged"
                  Foreground="#2DD4BF"/>
        </DockPanel>

        <CheckBox x:Name="AutoStartBox" Margin="0,0,0,8">
          <TextBlock Text="开机启动" Foreground="#ECF0F4"/>
        </CheckBox>
        <CheckBox x:Name="NotifyBox" Margin="0,0,0,12">
          <TextBlock Text="打印结束时通知" Foreground="#ECF0F4"/>
        </CheckBox>

        <TextBlock x:Name="StatusText" TextWrapping="Wrap" Foreground="#9AA3AD" FontSize="12"/>
      </StackPanel>
    </DockPanel>
  </Border>
</Window>
